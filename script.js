(function(){
 const root=document.getElementById('pem-app');
 const login=document.getElementById('login'), shell=document.getElementById('appShell'), view=document.getElementById('view');
 let page='dashboard', selectedProject='PRJ-001', selectedExpense=null, toastTimer; const BUDGET_WARNING_LIMIT=85;
 let projects=[
  {id:'PRJ-001',name:'Hodan Interlock Project',budget:10000,desc:'Construction of interlock roads in Hodan neighborhood.',expenses:[['EXP-001','20/08/2026','Materials',1000,'Ahmed Ali'],['EXP-002','21/08/2026','Transportation',500,'Ahmed Ali'],['EXP-003','22/08/2026','Labor',1000,'Ahmed Ali'],['EXP-004','23/08/2026','Materials',750,'Ahmed Ali']]},
  {id:'PRJ-002',name:'Waberi Road Project',budget:50000,desc:'Road construction project.',expenses:[['EXP-005','21/08/2026','Materials',12500,'Ahmed Ali'],['EXP-006','22/08/2026','Labor',8000,'Ahmed Ali'],['EXP-007','23/08/2026','Transportation',8000,'Ahmed Ali']]},
  {id:'PRJ-003',name:'Karaan Neighborhood',budget:40000,desc:'Neighborhood improvement project.',expenses:[['EXP-008','20/08/2026','Materials',7200,'Ahmed Ali'],['EXP-009','22/08/2026','Labor',8000,'Ahmed Ali']]},
  {id:'PRJ-004',name:'Daynile Building',budget:50000,desc:'Building and site works.',expenses:[['EXP-010','20/08/2026','Materials',12500,'Ahmed Ali'],['EXP-011','23/08/2026','Labor',13000,'Ahmed Ali']]}
 ];
 const money=n=>'$'+Number(n||0).toLocaleString(undefined,{minimumFractionDigits:0,maximumFractionDigits:2});
 const getP=id=>projects.find(p=>p.id===id);
 const totalExp=p=>p.expenses.reduce((s,e)=>s+Number(e[3]),0);
 const balance=p=>p.budget-totalExp(p);
 const used=p=>p.budget?Math.min(100,totalExp(p)/p.budget*100):0;
 function showToast(msg){clearTimeout(toastTimer);let old=root.querySelector('.pem-toast');if(old)old.remove();let t=document.createElement('div');t.className='pem-toast';t.textContent=msg;root.appendChild(t);toastTimer=setTimeout(()=>t.remove(),2500)}
  function showLimitWarning(p){
    const pct=p.budget?totalExp(p)/p.budget*100:0;
    if(pct < BUDGET_WARNING_LIMIT) return;
    let old=root.querySelector('.pem-limit-popup'); if(old)old.remove();
    let box=document.createElement('div');
    box.className='pem-limit-popup';
    box.innerHTML='<div class="pem-limit-box"><h2>Budget Limit Warning</h2><p>You have reached the limit.</p><div class="pem-limit-percent">'+pct.toFixed(1)+'% of budget used</div><p>Project: <b>'+p.name+'</b><br>Budget: <b>'+money(p.budget)+'</b><br>Spent: <b>'+money(totalExp(p))+'</b><br>Remaining: <b>'+money(balance(p))+'</b></p><button class="pem-btn pem-danger" id="closeLimitWarning">Close</button></div>';
    root.appendChild(box);
    document.getElementById('closeLimitWarning').onclick=()=>box.remove();
  }
 function nav(p){if(p==='logout'){shell.style.display='none';login.style.display='grid';return}page=p;root.classList.remove('pem-mobile-open');render()}
 function layout(head,body,actions=''){return '<div class="pem-pagehead"><div><h1>'+head+'</h1><p>'+body+'</p></div>'+actions+'</div>'}
 function render(){
  document.querySelectorAll('.pem-nav button').forEach(b=>b.classList.toggle('active',b.dataset.page===page));
  if(page==='dashboard')renderDashboard(); else if(page==='projects')renderProjects(); else if(page==='create')renderCreate(); else if(page==='details')renderDetails(); else if(page==='expense')renderExpense(); else if(page==='history')renderHistory(); else if(page==='receipt')renderReceipt(); else if(page==='reports')renderReports(); else if(page==='settings')renderSettings();
 }
 function renderDashboard(){const tb=projects.reduce((s,p)=>s+p.budget,0),te=projects.reduce((s,p)=>s+totalExp(p),0);let rows=projects.map(p=>'<tr class="clickable" data-open="'+p.id+'"><td><b>'+p.name+'</b></td><td>'+p.id+'</td><td>'+money(p.budget)+'</td><td>'+money(totalExp(p))+'</td><td class="pem-green-t"><b>'+money(balance(p))+'</b></td><td><span class="pem-status">Active</span></td></tr>').join('');let ex=projects.flatMap(p=>p.expenses.map(e=>({p,e}))).sort((a,b)=>b.e[1].localeCompare(a.e[1])).slice(0,6).map(x=>'<tr><td>'+x.e[0]+'</td><td>'+x.p.name+'</td><td>'+x.e[2]+'</td><td>'+money(x.e[3])+'</td><td>'+x.e[1]+'</td></tr>').join('');view.innerHTML=layout('Dashboard','Overview of all projects and expenses','<button class="pem-btn pem-primary" id="quickProject">+ New Project</button>')+'<div class="pem-cards"><div class="pem-card"><div class="pem-card-label">Total Projects</div><div class="pem-card-value pem-blue">'+projects.length+'</div></div><div class="pem-card"><div class="pem-card-label">Total Budget</div><div class="pem-card-value">'+money(tb)+'</div></div><div class="pem-card"><div class="pem-card-label">Total Expenses</div><div class="pem-card-value pem-red">'+money(te)+'</div></div><div class="pem-card"><div class="pem-card-label">Remaining Balance</div><div class="pem-card-value pem-green-t">'+money(tb-te)+'</div></div></div><div class="pem-grid2"><section class="pem-panel"><div class="pem-panel-head"><h3>Recent Projects</h3><button class="pem-link" id="allProjects">View All Projects</button></div><div class="pem-table-wrap"><table class="pem-table"><thead><tr><th>Project</th><th>ID</th><th>Budget</th><th>Expenses</th><th>Balance</th><th>Status</th></tr></thead><tbody>'+rows+'</tbody></table></div></section><section class="pem-panel"><div class="pem-panel-head"><h3>Recent Expenses</h3><button class="pem-link" id="reportLink">View Reports</button></div><div class="pem-table-wrap"><table class="pem-table"><thead><tr><th>No.</th><th>Project</th><th>Purpose</th><th>Amount</th><th>Date</th></tr></thead><tbody>'+ex+'</tbody></table></div></section></div>';bindCommon();document.getElementById('quickProject').onclick=()=>nav('create');document.getElementById('allProjects').onclick=()=>nav('projects');document.getElementById('reportLink').onclick=()=>nav('reports');view.querySelectorAll('[data-open]').forEach(el=>el.onclick=()=>{selectedProject=el.dataset.open;nav('details')})}
 function renderProjects(){view.innerHTML=layout('Projects','Manage and monitor all company projects','<button class="pem-btn pem-primary" id="newProject">+ New Project</button>')+'<div class="pem-toolbar"><input id="projectSearch" class="pem-search" placeholder="Search projects..."></div><section class="pem-panel"><div class="pem-table-wrap"><table class="pem-table"><thead><tr><th>Project Name</th><th>Project ID</th><th>Budget</th><th>Expenses</th><th>Balance</th><th>Status</th><th>Actions</th></tr></thead><tbody id="projectRows"></tbody></table></div></section>';bindCommon();document.getElementById('newProject').onclick=()=>nav('create');const fill=()=>{let q=document.getElementById('projectSearch').value.toLowerCase();document.getElementById('projectRows').innerHTML=projects.filter(p=>(p.name+' '+p.id).toLowerCase().includes(q)).map(p=>'<tr><td><b>'+p.name+'</b></td><td>'+p.id+'</td><td>'+money(p.budget)+'</td><td>'+money(totalExp(p))+'</td><td class="pem-green-t"><b>'+money(balance(p))+'</b></td><td><span class="pem-status">Active</span></td><td><button class="pem-btn pem-muted viewP" data-id="'+p.id+'">View</button></td></tr>').join('')||'<tr><td colspan="7" class="pem-empty">No projects found.</td></tr>';document.querySelectorAll('.viewP').forEach(b=>b.onclick=()=>{selectedProject=b.dataset.id;nav('details')})};document.getElementById('projectSearch').oninput=fill;fill()}
 function renderCreate(){view.innerHTML=layout('Create New Project','Add a new project and assign its initial budget.','<button class="pem-btn pem-muted" id="backProjects">← Back</button>')+'<section class="pem-panel"><form id="projectForm" class="pem-formgrid"><div><label class="pem-label">Project Name *</label><input id="pn" class="pem-field" required placeholder="Enter project name"></div><div><label class="pem-label">Project ID *</label><input id="pi" class="pem-field" required placeholder="PRJ-005"></div><div><label class="pem-label">Project Budget *</label><input id="pb" class="pem-field" type="number" min="0.01" step="0.01" required placeholder="10000"></div><div></div><div class="pem-field-full"><label class="pem-label">Description</label><textarea id="pd" class="pem-field" rows="4" placeholder="Optional project description"></textarea></div><div class="pem-field-full pem-form-actions"><button type="button" class="pem-btn pem-muted" id="cancelCreate">Cancel</button><button class="pem-btn pem-primary">Save Project</button></div></form></section>';bindCommon();document.getElementById('backProjects').onclick=()=>nav('projects');document.getElementById('cancelCreate').onclick=()=>nav('projects');document.getElementById('projectForm').onsubmit=e=>{e.preventDefault();let id=document.getElementById('pi').value.trim(),name=document.getElementById('pn').value.trim(),budget=Number(document.getElementById('pb').value);if(!name||!id||!Number.isFinite(budget)||budget<=0)return showToast('Please enter valid project information.');if(projects.some(p=>p.id.toLowerCase()===id.toLowerCase()))return showToast('Project ID already exists.');projects.push({id,name,budget,desc:document.getElementById('pd').value.trim(),expenses:[]});selectedProject=id;showToast('Project created successfully.');setTimeout(()=>nav('details'),400)}}
 function renderDetails(){let p=getP(selectedProject);if(!p)return nav('projects');let ex=totalExp(p),bal=balance(p),pct=used(p);let rows=p.expenses.slice().reverse().map(e=>'<tr><td>'+e[0]+'</td><td>'+e[1]+'</td><td>'+e[2]+'</td><td>'+money(e[3])+'</td><td>'+money(p.budget-p.expenses.slice(0,p.expenses.indexOf(e)+1).reduce((s,x)=>s+Number(x[3]),0))+'</td><td><button class="pem-btn pem-muted receiptBtn" data-id="'+e[0]+'">View</button></td></tr>').join('');view.innerHTML='<button class="pem-back" id="backP">← Back to Projects</button>'+layout(p.name,'Project ID: '+p.id,'<button class="pem-btn pem-green" id="addExp">+ Add Expense</button>')+'<div class="pem-cards"><div class="pem-card"><div class="pem-card-label">Project Budget</div><div class="pem-card-value">'+money(p.budget)+'</div></div><div class="pem-card"><div class="pem-card-label">Total Expenses</div><div class="pem-card-value pem-red">'+money(ex)+'</div></div><div class="pem-card"><div class="pem-card-label">Current Balance</div><div class="pem-card-value pem-green-t">'+money(bal)+'</div></div><div class="pem-card"><div class="pem-card-label">Budget Used</div><div class="pem-card-value pem-purple">'+pct.toFixed(1)+'%</div></div></div><section class="pem-panel" style="margin-bottom:16px"><div class="pem-panel-head"><h3>Budget Usage</h3><b style="font-size:11px">'+pct.toFixed(1)+'%</b></div><div class="pem-progress"><span style="width:'+pct+'%"></span></div><div style="display:flex;justify-content:space-between;font-size:10px;color:#718096"><span>'+money(ex)+' spent</span><span>'+money(bal)+' remaining</span></div></section><section class="pem-panel"><div class="pem-panel-head"><h3>Expense History</h3><button class="pem-link" id="allHistory">View All Expenses</button></div><div class="pem-table-wrap"><table class="pem-table"><thead><tr><th>Expense No.</th><th>Date</th><th>Purpose</th><th>Amount</th><th>Balance</th><th>Action</th></tr></thead><tbody>'+rows+'</tbody></table></div></section>';bindCommon();document.getElementById('backP').onclick=()=>nav('projects');document.getElementById('addExp').onclick=()=>nav('expense');document.getElementById('allHistory').onclick=()=>nav('history');view.querySelectorAll('.receiptBtn').forEach(b=>b.onclick=()=>{selectedExpense=b.dataset.id;nav('receipt')});if(pct>=BUDGET_WARNING_LIMIT)setTimeout(()=>showLimitWarning(p),250)}
 function renderExpense(){let p=getP(selectedProject),bal=balance(p);let next=()=>{let n=Number(document.getElementById('ea').value)||0;document.getElementById('prevBal').textContent=money(bal);document.getElementById('withdrawal').textContent='-'+money(n);document.getElementById('newBal').textContent=money(bal-n);document.getElementById('newBal').style.color=n>bal?'#b42318':'#159957'};view.innerHTML='<button class="pem-back" id="backD">← Back to Project</button>'+layout('Add Project Expense',p.name+' ('+p.id+')','')+'<div class="pem-two"><section class="pem-panel"><form id="expenseForm" class="pem-formgrid"><div><label class="pem-label">Expense / Invoice No. *</label><input id="en" class="pem-field" value="EXP-'+String(Date.now()).slice(-4)+'" required></div><div><label class="pem-label">Date *</label><input id="ed" class="pem-field" type="date" value="2026-08-24" required></div><div><label class="pem-label">Amount Withdrawn *</label><input id="ea" class="pem-field" type="number" min="0.01" step="0.01" placeholder="750" required></div><div><label class="pem-label">Purpose / Reason *</label><input id="ep" class="pem-field" placeholder="Construction Materials" required></div><div class="pem-field-full"><label class="pem-label">Description</label><textarea id="edesc" class="pem-field" rows="3" placeholder="Describe what the money is for"></textarea></div><div><label class="pem-label">Taken By *</label><input id="et" class="pem-field" value="Ahmed Ali" required></div><div><label class="pem-label">Reference / Invoice</label><input id="er" class="pem-field" placeholder="INV-102"></div><div class="pem-field-full"><label class="pem-label">Attachment (prototype)</label><input class="pem-field" type="file"></div><div class="pem-field-full pem-form-actions"><button type="button" class="pem-btn pem-muted" id="cancelExp">Cancel</button><button class="pem-btn pem-green">Save Expense</button></div></form></section><aside class="pem-balance"><h3 style="margin-top:0;font-size:14px">Balance Preview</h3><div class="pem-balance-row"><span>Previous Balance</span><b id="prevBal">'+money(bal)+'</b></div><div class="pem-balance-row"><span>Withdrawal</span><b id="withdrawal" style="color:#d94a45">-$0</b></div><div class="pem-balance-row total"><span>New Balance</span><b id="newBal" class="pem-green-t">'+money(bal)+'</b></div><div class="pem-note">The system calculates the balance automatically. Withdrawals above the available project balance are blocked.</div></aside></div>';bindCommon();document.getElementById('backD').onclick=()=>nav('details');document.getElementById('cancelExp').onclick=()=>nav('details');document.getElementById('ea').oninput=next;document.getElementById('expenseForm').onsubmit=e=>{e.preventDefault();let amount=Number(document.getElementById('ea').value),no=document.getElementById('en').value.trim(),purpose=document.getElementById('ep').value.trim(),taken=document.getElementById('et').value.trim();if(!no||!purpose||!taken||!Number.isFinite(amount)||amount<=0)return showToast('Please complete all required fields.');if(p.expenses.some(x=>x[0]===no))return showToast('Expense number already exists.');if(amount>balance(p))return showToast('Insufficient project balance.');p.expenses.push([no,document.getElementById('ed').value,purpose,amount,taken,document.getElementById('edesc').value,document.getElementById('er').value]);selectedExpense=no;showToast('Expense saved. Balance updated.');if(totalExp(p)/p.budget*100>=BUDGET_WARNING_LIMIT)setTimeout(()=>showLimitWarning(p),500);setTimeout(()=>nav('receipt'),700)}}
 function renderHistory(){let p=getP(selectedProject);view.innerHTML='<button class="pem-back" id="backD">← Project: '+p.name+'</button>'+layout('Expense History',p.name+' ('+p.id+')','<button class="pem-btn pem-green" id="addE">+ Add Expense</button>')+'<div class="pem-toolbar"><input id="expenseSearch" class="pem-search" placeholder="Search expenses..."><select id="purposeFilter" class="pem-field" style="max-width:180px"><option value="">All Purposes</option><option>Materials</option><option>Labor</option><option>Transportation</option></select></div><section class="pem-panel"><div class="pem-table-wrap"><table class="pem-table"><thead><tr><th>Expense No.</th><th>Date</th><th>Purpose</th><th>Amount</th><th>Balance</th><th>Taken By</th><th>Action</th></tr></thead><tbody id="histRows"></tbody></table></div></section>';bindCommon();document.getElementById('backD').onclick=()=>nav('details');document.getElementById('addE').onclick=()=>nav('expense');const fill=()=>{let q=document.getElementById('expenseSearch').value.toLowerCase(),f=document.getElementById('purposeFilter').value,cum=0;let arr=p.expenses.map(e=>{cum+=Number(e[3]);return {e,b:p.budget-cum}}).reverse().filter(x=>(x.e.join(' ').toLowerCase().includes(q))&&(!f||x.e[2]===f));document.getElementById('histRows').innerHTML=arr.map(x=>'<tr><td>'+x.e[0]+'</td><td>'+x.e[1]+'</td><td>'+x.e[2]+'</td><td>'+money(x.e[3])+'</td><td>'+money(x.b)+'</td><td>'+x.e[4]+'</td><td><button class="pem-btn pem-muted hrec" data-id="'+x.e[0]+'">View</button></td></tr>').join('')||'<tr><td colspan="7" class="pem-empty">No expenses found.</td></tr>';document.querySelectorAll('.hrec').forEach(b=>b.onclick=()=>{selectedExpense=b.dataset.id;nav('receipt')})};document.getElementById('expenseSearch').oninput=fill;document.getElementById('purposeFilter').onchange=fill;fill()}
 function renderReceipt(){let p=getP(selectedProject),e=p.expenses.find(x=>x[0]===selectedExpense)||p.expenses[p.expenses.length-1],idx=p.expenses.indexOf(e),prev=p.budget-p.expenses.slice(0,idx).reduce((s,x)=>s+Number(x[3]),0),curr=prev-Number(e[3]),total=Number(e[3]);let qrData=encodeURIComponent('HORSEED INTERLOCK COMPANY | '+p.id+' | '+e[0]+' | '+e[1]+' | '+total);view.innerHTML='<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:15px;gap:10px"><button class="pem-back" id="backHist">← Back to Project</button><div class="pem-inline-actions"><button class="pem-btn pem-primary" id="printRec">Print Invoice</button><button class="pem-btn pem-green" id="pdfRec">Save as PDF</button></div></div><div class="pem-invoice-shell" id="invoicePrint"><div class="pem-invoice-top"><div><div class="pem-invoice-company">HI<span style="font-size:12px">®</span></div><div style="font-size:10px;font-weight:800;margin-top:2px">HORSEED INTERLOCK COMPANY</div><div class="pem-invoice-contact" style="margin-top:8px">Phone: __________________<br>Address: __________________<br>Somalia</div></div><div class="pem-invoice-contact" style="padding-top:12px"><b>PROJECT EXPENSE MANAGEMENT</b><br>Expense &amp; Budget Control System<br>Project Expense Invoice</div><div class="pem-invoice-title"><img class="pem-invoice-qr" alt="Invoice QR" src="https://api.qrserver.com/v1/create-qr-code/?size=120x120&data='+qrData+'"><h2>INVOICE</h2><div class="pem-invoice-meta"><b>Invoice No:</b> '+e[0]+'<br><b>Date:</b> '+e[1]+'<br><b>Project ID:</b> '+p.id+'</div></div></div><div class="pem-invoice-rule"></div><div class="pem-invoice-bill"><div class="pem-invoice-block"><div class="pem-invoice-block-title">BILL TO</div><b>HORSEED INTERLOCK COMPANY</b><br>Project: '+p.name+'<br>Project ID: '+p.id+'</div><div class="pem-invoice-block"><div class="pem-invoice-block-title">EXPENSE DETAILS</div><b>Expense No:</b> '+e[0]+'<br><b>Taken By:</b> '+e[4]+'<br><b>Purpose:</b> '+e[2]+'<br><b>Reference:</b> '+((e[6]||'—'))+'</div></div><table class="pem-invoice-table"><thead><tr><th>No</th><th>Expense Code</th><th>Item / Purpose</th><th>Qty</th><th class="right">Rate</th><th class="right">Amount</th></tr></thead><tbody><tr><td>1</td><td>'+e[0]+'</td><td><b>'+e[2]+'</b><br>'+((e[5]||'Project expense'))+'</td><td>1</td><td class="right">'+money(total)+'</td><td class="right">'+money(total)+'</td></tr></tbody></table><div class="pem-invoice-bottom"><div class="pem-payment-box"><h3>PAYMENT / ACCOUNT DETAILS</h3><p>Payment Method: __________________<br>Account / Reference: '+((e[6]||'—'))+'<br>Recorded By: '+e[4]+'<br>Project Budget: '+money(p.budget)+'</p></div><div class="pem-total-box"><div class="pem-total-row"><span>Expense Total</span><b>'+money(total)+'</b></div><div class="pem-total-row"><span>Previous Balance</span><b>'+money(prev)+'</b></div><div class="pem-total-row"><span>Amount Withdrawn</span><b>-'+money(total)+'</b></div><div class="pem-total-row grand"><span>Current Balance</span><b>'+money(curr)+'</b></div></div></div><div class="pem-invoice-footer"><div><div class="line">Recorded By</div><div class="line">Signature</div></div><div><div class="line">Approved By</div><div class="line">Date</div></div></div><div class="pem-invoice-note">This document records a project expense against the stated project budget. The remaining balance is calculated automatically by the system.</div></div></div>';document.getElementById('backHist').onclick=()=>nav('details');document.getElementById('printRec').onclick=()=>window.print();document.getElementById('pdfRec').onclick=()=>{showToast('Print dialog opened — choose Save as PDF.');setTimeout(()=>window.print(),250)}}
 function renderReports(){let selected='all',from='',to='';view.innerHTML=layout('Reports','Generate project financial reports','')+'<section class="pem-panel"><div class="pem-formgrid"><div><label class="pem-label">Project</label><select id="rfProject" class="pem-field"><option value="all">All Projects</option>'+projects.map(p=>'<option value="'+p.id+'">'+p.name+'</option>').join('')+'</select></div><div></div><div><label class="pem-label">Date From</label><input id="rfFrom" class="pem-field" type="date"></div><div><label class="pem-label">Date To</label><input id="rfTo" class="pem-field" type="date"></div></div><div class="pem-form-actions"><button class="pem-btn pem-primary" id="genReport">Generate Report</button></div></section><div id="reportResult"></div>';bindCommon();function build(){selected=document.getElementById('rfProject').value;from=document.getElementById('rfFrom').value;to=document.getElementById('rfTo').value;let ps=selected==='all'?projects:projects.filter(p=>p.id===selected),tb=ps.reduce((s,p)=>s+p.budget,0),te=ps.reduce((s,p)=>s+totalExp(p),0),arr=ps.flatMap(p=>p.expenses.map(e=>({p,e}))).filter(x=>(!from||x.e[1]>=from.split('-').reverse().join('/'))&&(!to||x.e[1]<=to.split('-').reverse().join('/')));document.getElementById('reportResult').innerHTML='<div class="pem-report-summary"><div class="pem-card"><div class="pem-card-label">Total Budget</div><div class="pem-card-value">'+money(tb)+'</div></div><div class="pem-card"><div class="pem-card-label">Total Expenses</div><div class="pem-card-value pem-red">'+money(te)+'</div></div><div class="pem-card"><div class="pem-card-label">Remaining Balance</div><div class="pem-card-value pem-green-t">'+money(tb-te)+'</div></div><div class="pem-card"><div class="pem-card-label">Budget Used</div><div class="pem-card-value pem-purple">'+(tb?(te/tb*100).toFixed(1):'0')+'%</div></div></div><section class="pem-panel"><div class="pem-panel-head"><h3>Expense Details</h3><div class="pem-inline-actions"><button class="pem-btn pem-muted" id="printReport">Print Report</button><button class="pem-btn pem-green" id="pdfReport">Download PDF</button></div></div><div class="pem-table-wrap"><table class="pem-table"><thead><tr><th>Expense No.</th><th>Project</th><th>Purpose</th><th>Amount</th><th>Date</th><th>Taken By</th></tr></thead><tbody>'+arr.map(x=>'<tr><td>'+x.e[0]+'</td><td>'+x.p.name+'</td><td>'+x.e[2]+'</td><td>'+money(x.e[3])+'</td><td>'+x.e[1]+'</td><td>'+x.e[4]+'</td></tr>').join('')+'</tbody></table></div></section>';document.getElementById('printReport').onclick=()=>showToast('Print preview simulated for the prototype.');document.getElementById('pdfReport').onclick=()=>showToast('PDF download simulated for the prototype.')};document.getElementById('genReport').onclick=build;build()}
 function renderSettings(){view.innerHTML=layout('Settings','Basic prototype settings and company information','')+'<section class="pem-panel"><form id="settingsForm"><h3>Company Information</h3><div class="pem-formgrid"><div><label class="pem-label">Company Name</label><input id="companyName" class="pem-field" value="HORSEED INTERLOCK COMPANY" required></div><div><label class="pem-label">System Name</label><input id="systemName" class="pem-field" value="Project Expense Management System" required></div><div><label class="pem-label">Administrator Name</label><input id="adminName" class="pem-field" value="Ahmed Ali" required></div><div><label class="pem-label">Currency</label><select id="currency" class="pem-field"><option>USD ($)</option><option>EUR (€)</option><option>SOS (Sh)</option></select></div></div><div class="pem-form-actions"><button type="reset" class="pem-btn pem-muted">Reset</button><button class="pem-btn pem-primary">Save Settings</button></div></form><div class="pem-note">Settings are stored for this prototype session. User permissions, approvals and audit trails can be added in the production version.</div></section>';bindCommon();document.getElementById('settingsForm').onsubmit=e=>{e.preventDefault();document.querySelector('.pem-brand div:nth-child(2)').childNodes[0].textContent=document.getElementById('companyName').value+' ';document.querySelector('.pem-user').childNodes[0].textContent=document.getElementById('adminName').value;showToast('Settings saved successfully.')}}
 function bindCommon(){root.querySelectorAll('.pem-nav button').forEach(b=>{b.onclick=()=>nav(b.dataset.page)});let mm=document.getElementById('mobileMenu');if(mm)mm.onclick=()=>root.classList.toggle('pem-mobile-open');if(page==='projects'){document.querySelectorAll('.viewP').forEach(b=>{if(!b.parentElement.querySelector('.editP')){let e=document.createElement('button');e.className='pem-btn pem-muted editP';e.textContent='Edit';e.dataset.id=b.dataset.id;b.parentElement.appendChild(e);let d=document.createElement('button');d.className='pem-btn pem-danger deleteP';d.textContent='Delete';d.dataset.id=b.dataset.id;b.parentElement.appendChild(d)}});document.querySelectorAll('.editP').forEach(b=>b.onclick=()=>editProject(b.dataset.id));document.querySelectorAll('.deleteP').forEach(b=>b.onclick=()=>deleteProject(b.dataset.id))}if(page==='details'||page==='history'){document.querySelectorAll('.receiptBtn,.hrec').forEach(b=>{let p=b.parentElement;if(p&&!p.querySelector('.editE')){let e=document.createElement('button');e.className='pem-btn pem-muted editE';e.textContent='Edit';e.dataset.id=b.dataset.id;p.appendChild(e);let d=document.createElement('button');d.className='pem-btn pem-danger deleteE';d.textContent='Delete';d.dataset.id=b.dataset.id;p.appendChild(d)}});document.querySelectorAll('.editE').forEach(b=>b.onclick=()=>editExpense(b.dataset.id));document.querySelectorAll('.deleteE').forEach(b=>b.onclick=()=>deleteExpense(b.dataset.id))}}
 function editProject(id){let p=getP(id);if(!p)return;let name=prompt('Project name:',p.name);if(name===null)return;let budget=prompt('Project budget:',p.budget);if(budget===null)return;budget=Number(budget);if(!name.trim()||!Number.isFinite(budget)||budget<=0)return showToast('Invalid project information.');if(budget<totalExp(p))return showToast('Budget cannot be below existing expenses.');p.name=name.trim();p.budget=budget;showToast('Project updated.');render()}
 function deleteProject(id){let p=getP(id);if(!p)return;if(p.expenses.length)return showToast('Delete expenses first before deleting this project.');if(confirm('Delete '+p.name+'?')){projects=projects.filter(x=>x.id!==id);showToast('Project deleted.');nav('projects')}}
 function editExpense(id){let p=getP(selectedProject),e=p&&p.expenses.find(x=>x[0]===id);if(!e)return;let amount=prompt('Amount:',e[3]);if(amount===null)return;amount=Number(amount);let purpose=prompt('Purpose:',e[2]);if(purpose===null)return;let taken=prompt('Taken by:',e[4]);if(taken===null)return;if(!Number.isFinite(amount)||amount<=0||!purpose.trim()||!taken.trim())return showToast('Invalid expense information.');let old=e[3],newTotal=totalExp(p)-Number(old)+amount;if(newTotal>p.budget)return showToast('Expense would exceed project budget.');e[3]=amount;e[2]=purpose.trim();e[4]=taken.trim();showToast('Expense updated.');render()}
 function deleteExpense(id){let p=getP(selectedProject);if(!p)return;if(!confirm('Delete expense '+id+'?'))return;p.expenses=p.expenses.filter(e=>e[0]!==id);showToast('Expense deleted.');render()}
 document.getElementById('loginForm').onsubmit=e=>{e.preventDefault();let u=document.getElementById('loginUser').value.trim(),pw=document.getElementById('loginPass').value;if(u!=='Hassan Salad'||pw!=='Hassan@123')return showToast('Invalid username or password.');login.style.display='none';shell.style.display='flex';page='dashboard';render()};document.getElementById('forgot').onclick=()=>{let u=prompt('Enter your username:','admin');if(u!==null)showToast(u.trim()==='admin'?'Demo reset instructions sent.':'User not found.')};
})();
